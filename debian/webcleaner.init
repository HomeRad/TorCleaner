#!/bin/sh -e

PATH=/sbin:/bin:/usr/sbin:/usr/bin
NAME=webcleaner
DESC=WebCleaner
SERVICE=/service/$NAME

test -l $SERVER || exit 0

case "$1" in
  start)
	echo -n "Starting $DESC: "
        svc -u $SERVICE
        svc -u $SERVICE/log
	echo "$NAME."
	;;
  stop)
	echo -n "Stopping $DESC: "
	svc -d $SERVICE
        svc -d $SERVICE/log
	echo "$NAME."
	;;
  reload|force-reload)
        echo -n "Reloading $DESC: "
        svc -h $SERVICE
	echo "$NAME."
	;;
  restart)
        $0 stop
        $0 start
	;;
  status)
        svstat $SERVICE
	;;
  *)
	N=/etc/init.d/$NAME
	echo "Usage: $N {start|stop|restart|reload|force-reload|status}" >&2
	exit 3
	;;
esac

exit 0
